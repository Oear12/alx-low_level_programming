#include "main.h"

/**
 * _islower - checks for charcherif input in is a
 * lowercase char. another case, shows
 * 0
 *
 *
 * Return: 1 for lowercase char. 0 for the rest,
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (0);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
