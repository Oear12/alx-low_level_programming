#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * print_most_numbers - function checks for checks for a digit  (0 to 9)
 *
 * Return: always 0
 */

void print_most_numbers(void)
{
int c;
for (c = 48; c < 58; c++)
{
if (c != 50)
{
if (c != 52)
{
putchar(c = '0');
}
}
}
putchar('\n');
}
